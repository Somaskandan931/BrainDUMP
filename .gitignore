# Secrets — never commit these
.env
credentials.json
token.json
.env.example
# Python
__pycache__/
*.pyc
venv/
.venv/

# Local data (SQLite db, logs)
backend/data/
backend/logs/
qqqq
# Frontend has its own .gitignore (node_modules, .next, .env.local)
