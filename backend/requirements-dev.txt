# Test/dev-only dependencies -- run `pip install -r backend/requirements.txt -r backend/requirements-dev.txt`
pytest
httpx
# In-memory Redis for tests/security/test_rate_limit_redis.py -- exercises
# RedisSlidingWindowLimiter's real ZADD/ZREMRANGEBYSCORE/ZRANGE behavior
# without needing an actual Redis server in CI/sandboxes.
fakeredis
